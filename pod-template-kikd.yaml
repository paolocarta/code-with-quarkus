kind: Pod
metadata:
  labels:
    jenkins: "slave"
    jenkins/label: "tools"
spec:
  containers:
  - name: kikd
    image: clamer/kikd:1.2.2
    imagePullPolicy: IfNotPresent
    command:
    - cat
    tty: true
    volumeMounts:
      - name: registry-auth-config
        mountPath: /secret
      # - name: git-access
      #   mountPath: /home/jenkins/ssh-keys
  volumes:
    - name: registry-auth-config
      secret:
        secretName: registry-auth-config
    # - name: git-access
    #   secret:
    #     secretName: jenkins-git-ssh-key