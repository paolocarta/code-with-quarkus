kind: Pod
metadata:
  labels:
    jenkins: "slave"
    jenkins/label: "tools"
spec:
  containers:
  - name: kikd
    image: clamer/kikd:1.0.3
    imagePullPolicy: IfNotPresent
    # command:
    # - cat
    tty: true
  #   volumeMounts:
  #     - name: some-creds
  #       mountPath: /secret
  # volumes:
  #   - name: some-creds
  #     secret:
  #       secretName: some-creds